node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
*.log

# Playwright
test-results/
playwright-report/
.playwright/

# Supabase SQL (schema + seeds) kept locally only, not committed - run by hand
# in the SQL Editor. *.local.sql (real UUIDs + personal data) was never committed.
supabase/
*.local.sql

# Archived prototype contains personal data (name in filename, workout/diet
# spreadsheets). Kept locally for reference, excluded from the public repo.
legacy/
